# midul / libpackage

Enterprise-ready shared Go library scaffold for microservices.

This package contains only the files identified as "missing" from the planning document:
- additions under existing categories
- new root packages: `config`, `ratelimit`, `client`, `testing`

Business-domain packages such as coupon, reward, order, or travel are intentionally excluded.
